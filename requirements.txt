--index-url https://pypi.org/simple
--extra-index-url https://download.pytorch.org/whl/cu129

# --- API / backend ---
Flask==2.0.3
Flask-JWT-Extended==4.3.1
Flask-Swagger-UI==3.36.0
Flask-Cors==3.0.10
Werkzeug==2.0.3
PyJWT==2.9.0
python-dotenv==1.0.1
pymongo==4.3.3
paramiko>=3.0.0

# --- ML / utils ---
# évite pas mal d'emmerdes binaires
numpy<2

seqeval==1.2.2
scikit-learn==1.3.2
deepmerge==1.1.1

# contraintes imposées par ton env (paddlex / roboflow)
PyYAML==6.0.2
opencv-python-headless==4.10.0.84

ultralytics>=8.3.190,<9.0.0
PyMuPDF>=1.24.0
pillow>=10.0.0

# --- PyTorch CUDA 12.9 (cu129) ---
# torchvision 0.24.1 est compatible avec PyTorch 2.9.1
torch==2.9.1
torchvision==0.24.1

# --- HF / MinerU / GLiNER2 ---
# mineru-vl-utils[transformers] demande transformers >= 4.56
# et on évite d'installer transformers v5 (breaking changes)
einops>=0.7.0
mineru-vl-utils[transformers]

# --- Constrained JSON decoding ---
lm-format-enforcer>=0.9.8
jsonschema>=4.0.0

gliner2==1.2.3
gliner==0.2.24

transformers==4.57.3
huggingface-hub>=0.34.0,<1.0
accelerate>=0.33.0,<2.0
pydantic>=2.7,<3